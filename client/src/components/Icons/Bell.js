import React from 'react';
import { NotificationsNoneOutlined } from '@mui/icons-material';

export const BellIcon = (props) => (
  <NotificationsNoneOutlined sx={{ fontSize: '2rem' }} {...props} />
);
