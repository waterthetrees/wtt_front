import React from 'react';
import SearchIcon from '@mui/icons-material/Search';

export const Search = (props) => {
    return (
        <>
            <SearchIcon
                {...props} 
            />
        </>
    )
}